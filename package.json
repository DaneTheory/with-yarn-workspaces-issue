{
  "private": true,
  "engines": {
    "node": "12.x",
    "yarn": ">=2.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace web-app run dev",
    "build": "yarn workspace web-app run build",
    "start": "yarn workspace web-app run start",
    "now": "yarn workspace web-app run now"
  },
  "devDependencies": {
    "now": "^17.1.1"
  }
}
